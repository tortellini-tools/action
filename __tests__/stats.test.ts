describe('collect_stats', () => {
    describe('when given a HTML file with without ort-report-data blob', () => {
        let webapp_file: string

        beforeEach(() => {
            // TODO fill webapp_file
        })

        it('should return an empty stats object', () => {
            const stats = collect_stats(webapp_file)
        })
    })
})
